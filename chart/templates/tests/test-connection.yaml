apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liberty-app.fullname" . }}-test-connection"
  labels:
    {{- include "liberty-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liberty-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
